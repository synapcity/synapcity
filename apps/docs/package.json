{
  "name": "synapcity-docs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "mint dev --port 3333",
    "build": "echo \"(build handled by Mintlify Cloud)\" && exit 0",
    "start": "mint dev --port 3333"
  }
}
